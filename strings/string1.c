#include<stdio.h>
#include<stdlib.h>

int main() {
	char *ch;
	ch = (char*)malloc(100*sizeof(char));
	printf("Enter a word:");
	/*gets(ch);
	
	for (int i = 0; ch[i]!='\0'; i++) {
		printf("%c", ch[i]);
	}
	printf("\n");
	printf("%s",ch);
	printf("\n");*/
	char = ch;
	for (int i =o ; (ch = getchar())!= '\n'; i++) {
		a[i] = ch;
	}
	printf("%s\n",a);
	return 0;
}
